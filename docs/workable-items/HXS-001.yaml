---
id: HXS-001
title: "Angular SPA root page missing app-root or router-outlet"
status: closed
severity: important
source: "hxs_frontend_e2e"
challenge_run: "2026-07-24T19:52:51Z"
description: >
  Auto-detected failure from hxs_frontend_e2e during run hxs_run_1.
  Angular SPA root page missing app-root or router-outlet
resolution:
  summary: >
    The E2E script used curl to check for Angular-rendered content in HTML
    responses, which does not work for SPAs that render via JavaScript.
    Updated hxs_frontend_e2e.sh to check HTTP 200 + SPA shell elements
    (app-root, router-outlet) instead of rendered content.
  fix_commit: 7b02f08
  verified: "14 PASS, 0 FAIL across 12 Angular routes + 2 SPA shell checks"
recordings: []
findings:
  - root cause: E2E script used curl content checks on an SPA, which cannot work since Angular renders via JS in the browser
  - fix: Switch to HTTP 200 status check per route + presence of SPA shell elements (app-root, router-outlet)
  - verified_by: hxs_frontend_e2e.sh run in HXS orchestrator — all 14 assertions pass
root_cause: "E2E script curl content checks incompatible with Angular SPA rendering model"
fix:
  pr: ""
  files:
    - tests/challenges/scripts/hxs_frontend_e2e.sh
  verified_by: "hxs orchestrator run 2026-07-24T19:53:36Z"
related_items: []
